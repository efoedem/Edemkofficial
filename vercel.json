{
  "builds": [
    {
      "src": "QUIZ1/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "QUIZ1/wsgi.py"
    }
  ],
  "scripts": {
    "vercel-build": "pip install -r requirements.txt && python manage.py migrate --noinput"
  }
}